import { DoctorSearchPipe } from './doctor-search.pipe';

describe('DoctorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
